---
- name: Download kubectl binary
  ansible.builtin.get_url:
    url: "https://dl.k8s.io/release/{{ kubectl_version }}/bin/linux/{{ system_arch }}/kubectl"
    dest: "{{ kubectl_install_path }}"
    mode: "0755"
    # Last-Modifiedベースの鮮度判定はバージョン切り替え時に誤って「変更なし」と判定することがあるため、
    # チェックサムで確実に対象バージョンかどうかを検証する
    checksum: "sha256:{{ lookup('ansible.builtin.url', 'https://dl.k8s.io/release/' + kubectl_version + '/bin/linux/' + system_arch + '/kubectl.sha256', split_lines=False) }}"

- name: Verify kubectl client version
  ansible.builtin.command: "{{ kubectl_install_path }} version --client"
  register: kubectl_version_check
  changed_when: false

- name: Show kubectl version
  ansible.builtin.debug:
    msg: "{{ kubectl_version_check.stdout_lines | default([kubectl_version_check.stdout]) }}"
