apiVersion: v1 # 使用するAPIのバージョン
kind: Secret # 機密データを保存する（演習用のダミー値）
metadata: # オブジェクトを一意に特定するための情報
  name: study-db-secret # Secret名
type: Opaque # 任意のユーザー定義データ
stringData: # 平文で書ける（適用時にエンコードされる）
  username: "demo_user"
  password: "change-me-in-real-use" # 本番では外部シークレット管理を使う
