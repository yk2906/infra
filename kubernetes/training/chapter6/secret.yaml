apiVersion: v1 # 使用するAPIのバージョン
kind: Secret # 機密値（ダミー。本番では外部シークレット管理を検討）
metadata: # オブジェクトを一意に特定するための情報
  name: capstone-secret
  namespace: capstone-training
type: Opaque
stringData:
  api_token: "replace-me"
  # TODO: DB接続文字列など必要なキーを追加
