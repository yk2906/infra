apiVersion: v1 # 使用するAPIのバージョン
kind: ConfigMap # アプリ設定（必要に応じてキーを増やす）
metadata: # オブジェクトを一意に特定するための情報
  name: capstone-config
  namespace: capstone-training # namespace.yaml と一致
data:
  APP_NAME: "capstone-demo"
  # TODO: 演習で追加する設定キー
