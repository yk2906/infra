apiVersion: v1 # 使用するAPIのバージョン
kind: ServiceAccount # Pod や外部クライアントに紐づくアイデンティティ
metadata: # オブジェクトを一意に特定するための情報
  name: study-viewer # 読み取り専用演習用SA
  namespace: default # 別ネームスペースで試す場合は変更し、Role/RoleBindingも合わせる
