apiVersion: apps/v1
kind: Deployment
metadata:
  name: steam-bot
  labels:
    app: steam-bot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: steam-bot
  template:
    metadata:
      labels:
        app: steam-bot
    spec:
      containers:
      - name: bot-container
        image: steam-bot-image:latest
        imagePullPolicy: Always
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
        env:
          - name: APP_ENV
            value: "production"
          - name: DISCORD_TOKEN
            valueFrom:
              secretKeyRef:
                name: discord-token
                key: token
          - name: STEAM_API_KEY
            valueFrom:
              secretKeyRef:
                name: discord-token
                key: steam_key
        ports:
          - containerPort: 8080